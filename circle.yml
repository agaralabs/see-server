machine:
    node:
        version: 6.1.0
    java:
        version: oraclejdk8

dependencies:
    override:
        - gem install scss_lint
        - .bin/dependencies.sh

test:
    override:
        - .bin/test.sh
        - .bin/build.sh
