machine:
    node:
        version: 6.1.0
    java:
        version: oraclejdk8

dependencies:
    override:
        - gem install scss_lint
        - .bin/dependencies.sh
    cache_directories:
        - "dashboard/server/npm_modues"
        - "dashboard/client/npm_modues"

test:
    override:
        - .bin/test.sh
        - .bin/build.sh
