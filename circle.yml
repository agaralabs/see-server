machine:
    node:
        version: 6.1.0
    java:
        version: oraclejdk8

dependencies:
    override:
        - gem install scss_lint
        - .bin/dependencies.sh
    cache_directories:
        - "dashboard/server/npm_modules"
        - "dashboard/client/npm_modules"

test:
    override:
        - .bin/test.sh
        - .bin/build.sh
